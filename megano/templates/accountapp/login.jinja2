{% extends 'base.jinja2' %}

{% block header %}
    {% include 'headers/header_control.jinja2' %}
    {% include 'headers/header_logo.jinja2' %}
{% endblock %}

{% block content %}
<div class="wrap">
            <div class="wrapper">
                <div class="header">
                    <h3 class="sign-in">Вход</h3>
                </div>
                <div class="clear"></div>
                    <form action="#" method="post">
                    {% csrf_token %}
                    <div>
                        {% if error_message %}
                            <span class="error-text" style="color: rgb(255, 0, 0);">{{ error_message }}</span>
                        {% endif %}
                        <input class="user-input" type="text" name="email" id="email" placeholder="E-mail">
                    </div>
                    <div>
                        <input type="password" name="password" id="name" placeholder="*********">
                    </div>
                    <div>
                        <input type="submit" value="Войти">
                    </div>
                    <a class="forgot-label" href="{{ url('accountapp:password_reset') }}">Забыли пароль?</a>
                    <a class="registr" href="{{ url('accountapp:registration') }}">Регистрация</a>
                </form>
            </div>
        </div>
{% endblock %}
